# CLAUDE.md — 大阪石材 AIチャットボット・管理画面

このファイルは、このリポジトリで作業する際に常に参照する要件・設計判断・用語ルールの記録です。
今後のフェーズ作業でも、まずこのファイルを読んでから着手してください。

## 0. プロジェクトの目的

- 大阪石材（https://www.osaka-sekizai.jp/）のホームページに設置する「AIチャットボット」と、その「管理画面」を新規開発する。
- 現在利用中のクラウドサーカス IZANAI の置き換えが目的。年間コストを下げつつ、ITに詳しくない社員でも直感的に運用できることを最優先にする。
- 利用者は2種類。
  - お客さま：サイト訪問者。お墓じまい、戒名彫刻、お墓の引越し・リフォームなどを質問する。
  - 運用担当者：ITに詳しくない社員。管理画面で情報登録・FAQ追加・見た目変更・公開を行う。
- 想定規模：月数千会話程度。まず小さく作り、あとから拡張できる設計にする。

## 1. 本セッションのスコープ（重要）

**今回（このセッション）はUI/UXの試作のみ。** 本番用のバックエンドや外部API（Supabase、Anthropic Claude API、Voyage AI等）は一切使わない。

- データはすべてブラウザ内の仮データ（`localStorage` + 初期シードデータ）で動かす。サーバーAPI・DBは実装しない。
- AIの回答は「FAQとの簡易一致によるダミー応答」で代替する。実際のLLM呼び出しは行わない。
- のちに本物のAPI（データ取得はSupabase等、回答生成はClaude API等）へ差し替えやすいように、**データ取得層**（`src/lib/data/`）と **回答生成層**（`src/lib/answer/`）を明確に分離して実装する。呼び出し側（UIコンポーネント）は、この2層の関数シグネチャ（Promiseを返す非同期関数）だけに依存し、内部実装（localStorageか実APIか）を意識しない。
- したがって、下記「9. フェーズ分け」のうちフェーズ1の**機能・画面のみ**を対象とし、しかも「チャットAPI」は本物のAPIではなくダミー実装とする。

### 差し替えの指針

- `src/lib/data/sources.ts` … 現在は localStorage 上の配列を操作。将来は `fetch('/api/sources')` 等に置き換える。関数シグネチャ（引数・返り値の型）は変えずに中身だけ差し替えられるようにする。
- `src/lib/answer/generateAnswer.ts` … 現在はダミーFAQ配列に対する文字列一致（部分一致・キーワード一致）で回答を返す。将来は Claude API 呼び出し + RAG検索に差し替える。呼び出し側は `generateAnswer(question, context)` の型だけに依存する。

## 2. 技術スタック（今回のフェーズで採用しているもの）

- Next.js（App Router）+ TypeScript + Tailwind CSS v4（`@theme` によるトークン定義。`tailwind.config.ts` は使わず `src/app/globals.css` にテーマを集約）
- 状態・永続化：React state + `localStorage`（仮データ用。SSR時は初期値、クライアントでhydrateする）
- 本番想定のスタック（Supabase / Claude API / Voyage AI / pdf-parse 等）は **未実装**。CLAUDE.md・コメントで置き場所だけ示す。

## 3. 用語の置き換えルール（画面に出す言葉）

管理画面のUI文言・コンポーネント名・コメントで、内部用語をそのまま出さない。必ず下記に置き換える。

| 内部の用語 | 画面に出す言葉 |
| --- | --- |
| 学習データ / ナレッジ / RAG | AIに覚えさせる情報 |
| チャンク化・ベクトル化・学習 | 読み込み |
| チャット参照 ON/OFF | チャットで使う／使わない |
| Q&A | よくある質問 |
| 未回答ログ / フォールバック | 答えられなかった質問 |
| システムプロンプト | 話し方・ルール |
| デプロイ / 本番反映 | 公開する |
| ロールバック | この版に戻す |
| ウィジェット | チャット（お客さまから見える部分） |

## 4. デザインルール

- 背景：暖かみのあるオフホワイト `#F6F5F1`。カードは白＋細い境界線。角丸 10〜14px。
- メインカラー：紺 `#1F3A7A`（サイトロゴの色）。危険操作（削除等）だけ赤茶 `#9A3412`。
- フォント：本文 = Zen Kaku Gothic New、見出し = Zen Maru Gothic（`next/font/google` で読み込み、CSS変数化）。
- アクセシビリティ：本文コントラスト4.5:1以上、押せる要素は44px以上、ボタンは`<button>`、入力欄には`<label>`、アイコンのみボタンには`aria-label`。キーボード操作・フォーカス可視を担保する。
- 日本語入力配慮：Enterで送信/追加する箇所は必ず `isComposing`（変換確定のEnter）を判定し、誤送信を防ぐ。
- レイアウト：PC優先（1280px以上）。管理画面は 左サイドメニュー(約250px) / 中央コンテンツ / 右テスト画面(約420px) の3カラム。スマホは最低限閲覧可能にする。

## 5. 管理画面の基本方針

- ITに詳しくない人が説明なしで使えることを最優先。専門用語は出さない（3節の表を厳守）。
- 1画面1目的。画面上部に画面名と「この画面で何ができるか」を1行で明記する。
- 下書きと公開の分離はフェーズ2、公開履歴・この版に戻すはフェーズ3で実装済み。「AIに覚えさせる情報」画面の追加・削除・スイッチ操作は一貫して即時反映（下書き概念を持たない。理由は10節）。
- 削除は確認モーダルを出さず即実行し、「元に戻す」付きのトースト通知を出す。
- 操作結果は必ずトースト通知で伝える。
- 一覧が空のときは次にすべきことを文言で案内する。
- 自由記述は最小限。選べるところは選択肢（プリセット）にする。

## 6. 今回実装した画面・機能（フェーズ1・ダミー版）

- `/admin` ホーム：3ステップ（①情報を登録→②テストで確認→③公開する）の案内。③公開するはフェーズ2以降の機能のため、ここでは案内のみでリンク先は用意しない。
- `/admin/sources`「AIに覚えさせる情報」：URL／PDF／テキストの3種類の追加、一覧（種類・名前・サイズ・状態・チャットで使うスイッチ・再読み込み・削除）、読み込み中→読み込み済みの疑似非同期処理。
- 管理画面右側「テスト画面」：常設。ダミー回答生成を使って会話できる。フェーズ1では「答えられなかった質問」の記録画面自体は作らないため、コンソール記録程度に留める（画面は作らない）。
- `/widget-demo`：お客さま向けチャットのUI確認用デモページ。実際の `<script>` 埋め込みではなく、同等の見た目・挙動をNext.jsページ上で再現したもの。

## 7. 今回のフェーズで作っていないもの（意図的に対象外）

- Supabase Auth・DB・Storage、Claude API、Voyage AI、pdf-parseによる実PDF解析、sitemap.xmlクロール → 本物のAPI差し替え時に実装。
- 実際のログイン・招待メール送信、レート制限、ドメイン制限、RLS等のセキュリティ・運用機能 → バックエンド実装フェーズで対応。
- 会話ログ・利用状況の自動集計（保存期間に基づく自動削除、API利用料からの実際の金額算出）→ 本実装でバックエンド処理として追加。

（よくある質問・答えられなかった質問・話し方や案内先・見た目・下書きと公開はフェーズ2、公開履歴・この版に戻す・メンバーと権限・会話ログ・利用状況と上限はフェーズ3で実装済み。9節・11節参照）

## 8. 妥当な判断で進めた軽微な決定

- Tailwind v4の`@theme`でCSS変数を定義し、`tailwind.config.ts`は生成しない（v4の標準構成に合わせた）。
- ダミーFAQデータは要件5-4の初期FAQ5件をそのまま使用し、`src/lib/answer/dummyFaq.ts` に配置。
- 「AIに覚えさせる情報」の初期データはトップページURLと価格表PDF（ファイル名のみのダミー）の2件とした。
- PDFアップロードは実際のファイル解析を行わず、ファイル名・サイズ(KB)のみを読み取って登録する（テキスト抽出はダミー本文を挿入）。
- テスト画面はページ遷移してもリセットされないよう `localStorage` に会話を保持せず、コンポーネントstateのみで保持（会話ログ永続化は未実装のフェーズのため）。

## 9. フェーズ2で追加した画面・機能

- `/admin/faq`「よくある質問」：追加・編集フォーム（お客さまの質問／AIに答えてほしい内容）、キーワード検索、編集・削除（削除は「元に戻す」付きトースト）。初期5件は質問文のみで回答は空欄（担当者が入力するまでチャットには使われない）。
- `/admin/unanswered`「答えられなかった質問」：同じ質問文はまとめて回数・最終日時を表示。「回答を書く」→「よくある質問に追加」で一覧から消える。「対応しない」は非表示（元に戻すトースト付き）。サイドメニューに件数バッジ。
- `/admin/persona`「話し方・ルール」：口調（丁寧／親しみやすい／簡潔、例文カード）、答えさせない話題（初期4件のチェックボックス＋自由追加）、案内先（電話番号・お問い合わせURL）。
- `/admin/appearance`「見た目」：テーマ色（5プリセット＋自由指定）、表示位置（右下／左下）、アイコン画像、チャットの名前・呼び出しボタンの文字・あいさつ・注意書き、候補ボタン（最大6個）。
- `/admin/publish`「公開と履歴」：未公開の変更を一覧表示し「この内容で公開する」。公開履歴・この版に戻すはフェーズ3で追加予定の案内のみ。
- ヘッダー（`PageHeader`）を全画面共通で動的化：「未公開の変更 ◯件」／「公開中の内容と同じです」と「公開する」ボタンを常設。
- ホーム画面に「答えられなかった質問 ◯件［確認する］」「未公開の変更 ◯件［公開画面へ］」の2枚のカードを追加。

## 10. フェーズ2の設計判断

- **下書き/公開の対象範囲**：よくある質問・話し方や案内先・見た目の3つだけを下書き（`draft`）／公開（`published`）の二層構成にした（`src/lib/data/faqs.ts` `persona.ts` `appearance.ts`）。「AIに覚えさせる情報」（sources）は、今回のダミー回答生成がFAQのみを参照し、sourcesの内容はお客さま向け表示に影響しないため、フェーズ1と同じ即時反映のままとした（下書きのカウント対象外）。本実装でRAG検索を組み込む際は、sourcesも下書き/公開の対象にする想定。
- **「未公開の変更」の数え方**：draftとpublishedのオブジェクトを都度diffするのではなく、編集操作のたびに`recordDraftChange(summary)`で変更点を1件ずつログに記録する方式にした（`src/lib/data/publishState.ts`）。「公開する」でログをクリアする。シンプルだが、`/admin/publish`に変更点の一覧をそのまま表示できる利点がある。
- **テスト画面 と お客さま向けチャットの出し分け**：`generateAnswer` / `streamAnswer` / `useChatConfig` に `isTest` を渡し、trueなら下書き（FAQ・話し方・見た目）、falseなら公開版を参照する。テスト画面は常に`isTest=true`、`/widget-demo`（お客さま向け）は常に`isTest=false`。
- **FAQ一致ロジック**：フェーズ1のキーワード配列を廃止し、質問文同士の正規化＋部分一致＋文字2-gram重なりで簡易スコアリングする方式に変更（`src/lib/answer/generateAnswer.ts`）。回答が空欄のFAQは候補から除外する。
- **答えさせない話題の判定**：チェックされた話題ラベルごとに内部の連想キーワード（コード内の`PRESET_TOPIC_HINTS`）で簡易マッチングする。自由追加した話題はラベル文字列そのものでマッチングする（簡易実装）。該当時は`refused: true`を返し、`unanswered`（答えられなかった質問への記録対象）とは区別する。
- **口調の反映範囲**：ダミー回答生成ではFAQの回答文はそのまま使うため、口調が実際に効くのは「お答えできない」「話題を断る」の定型文のみ（`FALLBACK_TEMPLATES` / `REFUSAL_TEMPLATES`）。本実装でClaude APIに差し替えるときは、口調をシステムプロンプトに反映する。
- **見た目のテーマ色**：管理画面自体（サイドメニュー等）は常に紺 `#1F3A7A` 固定とし、見た目設定で変更できるのはチャット部分（`ChatPanel`等）の配色のみ。CSS変数ではなくコンポーネントのinline styleで反映している（プリセット外の自由指定に対応するため）。
- **アイコン画像**：実ファイルはアップロードせず、`FileReader`で読み取ったdata URLを`localStorage`にそのまま保存する簡易実装。
- **並び順のバグ修正**：フェーズ1で作成日時が同一の初期データを`sort((a,b)=>a.createdAt<b.createdAt?1:-1)`で並べ替えていたため、同着時に比較関数の非対称性で順序が不安定になり、一覧が意図せず逆順で表示される不具合があった。`src/lib/utils/sort.ts`の`compareDesc`（同値なら0を返す）に統一して修正した（sources・faqs・unanswered・publishStateすべてに適用）。

## 11. フェーズ3で追加した画面・機能

- `/admin/publish`「公開と履歴」に公開履歴セクションを実装：公開する・この版に戻すのたびに版（日時・公開した人・変更点）を記録し、一覧表示する。「この版に戻す」で過去のよくある質問・話し方や案内先・見た目のスナップショットを下書き・公開版の両方に復元する（元の版は消さず、新しい版として履歴に追加する＝ロールバックのロールバックも可能）。
- `/admin/members`「メンバー」：メールアドレスでの招待、権限（編集できる／見るだけ）の一覧表示・変更・削除（元に戻すトースト付き）。動作確認用に「自分の権限を切り替える」ボタンを設置（本実装ではログイン中のメンバーの権限がそのまま使われるため不要になる）。
- `/admin/conversations`「会話ログ」：お客さま向けチャット（`isTest=false`）の会話のみを表示。日付・キーワードで検索し、1件ずつ全文を確認できる。電話番号・メールアドレスらしき文字列は自動的に伏せて表示する。保存期間（30/90/180/365日）を設定できる（この試作では自動削除は行われない）。
- `/admin/usage`「利用状況」：今月の利用状況（ダミー値）と上限をバーで表示、月の利用上限・上限到達時にお客さまへ表示する文言・通知先メールアドレスを設定。動作確認用に「上限に達した状態を試す」チェックボックスがあり、ONにするとテスト画面・お客さま向けチャットの両方で、その他の処理より前に上限到達の案内文だけを返すようになる。
- 「見るだけ」権限の反映：`useCanEdit()`（`src/lib/data/viewerRole.ts`を参照）で、AIに覚えさせる情報・よくある質問・答えられなかった質問・話し方ルール・見た目・利用状況・メンバー・公開の各画面の主要な操作ボタン・入力欄を無効化し、`ReadOnlyNotice`を表示する。サイドメニュー下部にも「見るだけの権限で表示中」の案内を常設。

## 12. フェーズ3の設計判断

- **公開履歴の保存方法**：公開のたびに、よくある質問・話し方や案内先・見た目の内容をまるごとスナップショットとして`src/lib/data/publishHistory.ts`に保存する（差分ではなく全体）。件数が増えると`localStorage`を圧迫するが、試作段階では問題にならない規模のため許容した。本実装ではDBに保存し、件数・保存期間の上限を設ける想定。
- **この版に戻す＝新しい公開**：ロールバックは過去のレコードを直接指す（ポインタを戻す）のではなく、その内容で新しい版を作って公開する方式にした。これにより「この版に戻す」を押した後でも、それ以前の版に再度戻せる（履歴が失われない）。
- **メンバーの権限とAIに覚えさせる情報の関係**：「見るだけ」でも情報の閲覧はできるが、変更系の操作（追加・編集・削除・保存・公開・この版に戻す）はすべて無効化している。検索・絞り込みなど閲覧系の操作は「ログの閲覧のみ」の趣旨に沿って有効のままにしている。
- **自分の権限の切り替えUI**：本来はログインしているメンバー自身の権限がそのまま適用されるべきだが、今回はログイン機能がないため、`/admin/members`に「動作確認用」と明記した切り替えボタンを設けた。本実装（Supabase Auth導入）でこのUIは削除し、ログイン中のメンバー情報から権限を判定するようにする。
- **会話ログとテスト画面の分離**：`src/lib/data/conversations.ts`で`isTest`ごとに別データとして保存し、`/admin/conversations`は`isTest=false`のみを表示する。あいさつだけで終わった訪問を記録しないよう、実際にお客さまが最初の質問を送った時点でまとめて会話を開始する（あいさつ＋最初の質問を1回の書き込みで記録）。
- **PIIマスキング**：`src/lib/utils/maskPii.ts`で電話番号・メールアドレスらしき文字列を正規表現で伏せる簡易実装。会話ログの表示時にのみ適用し、保存データ自体はマスキングしない（将来、より精度の高い検出方式に差し替えやすいように、表示直前の変換として実装している）。
- **利用上限の判定順序**：`generateAnswer()`の先頭で利用上限チェックを行い、上限到達時はFAQ検索・答えさせない話題判定より前に案内文を返す（`unavailable: true`、`unanswered: false`）。この状態は「答えられなかった質問」には記録しない（内容の問題ではなく運用上の理由のため）。
- **利用状況・上限設定は下書き/公開の対象外**：緊急時に即座に反映させたい設定のため、保存すると下書き・公開の区別なくすぐに反映される（`src/lib/data/usage.ts`）。同じ理由でメンバー・権限も下書き/公開の対象外としている。
- **通知先メールの初期値**：セッションのユーザーのメールアドレスを、初期メンバー（自分）および利用上限の通知先メールアドレスの初期値として使用した（実際の送信は行わない）。
